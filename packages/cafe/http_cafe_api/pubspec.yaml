name: http_cafe_api
description: A new Flutter package project.
version: 0.0.1

publish_to: "none"

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.6
  local_data:
    path: ../../local_data
  cafe_api:
    path: ../cafe_api
  rxdart: ^0.27.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter: null