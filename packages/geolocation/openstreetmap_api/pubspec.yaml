name: openstreetmap_api
description: A new Flutter package project.
version: 0.0.1

publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  geolocation_api:
    path: ../geolocation_api
  http: ^1.0.0
  local_data:
    path: ../../local_data

dev_dependencies:
  mockito: ^5.4.0
  flutter_lints: ^2.0.0
  build_runner: ^2.3.3
  flutter_test:
    sdk: flutter

flutter: null
